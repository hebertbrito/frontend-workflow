language: generic
sudo: required
services:
  - dockerfile

before_install:
  - docker build -t britto/frontend-workflow -f Dockerfile.dev .

script:
 - docker run -e CI=true britto/frontend-workflow npm run test